name: RaspBerryPiWeb
on:
  push:
    branches:
      - main
  workflow_dispatch: null
jobs:
  build:
    runs-on: self-hosted
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Enter directory
        run: cd app
      - name: Intall modules
        run: npm i